subprojects {
    apply plugin: 'java'
    
    repositories {
		mavenCentral()
	}

    dependencies {
		testCompile("junit:junit:4.11")
	}
}

project("koara-html5") {
	dependencies {
		compile(project(":koara-core"))
	}
}

project("koara-xml") {
	dependencies {
		compile(project(":koara-core"))
	}
}